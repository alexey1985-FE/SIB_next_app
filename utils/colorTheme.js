import { createTheme } from '@material-ui/core';

export const colorTheme = createTheme({
	palette: {
		primary: {
			main: '#fff',
		},
		secondary: {
			main: '#C65D26',
		},
		info: {
			main: '#5E8CD9',
		},
	},
});
